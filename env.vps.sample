# VPS側メール送信API用 環境変数設定
# このファイルを VPS 上で .env としてコピーしてください

# メール送信API サーバー設定
EMAIL_API_PORT=3001
NODE_ENV=production

# Xserver SMTP設定
SMTP_HOST=sv10210.xserver.jp
SMTP_PORT=587
SMTP_USER=contact@kanucard.com
SMTP_PASS=Kanu29012300

# メール送信元アドレス
FROM_EMAIL=contact@kanucard.com
